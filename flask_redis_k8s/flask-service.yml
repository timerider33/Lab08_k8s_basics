apiVersion: v1
kind: Service

metadata:
  name: service-devops
  labels:
    app: flask-app

spec:
  type: LoadBalancer

  selector:
    app: flask-app
    svc: front

  ports:
    - name: http
      protocol: TCP
      port: 8000
      targetPort: 5000
      
externalIPs:
  - 10.0.22.2
